import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

import ChoiceListHelper from '../helpers/ChoiceListHelper';
import { Choices } from './constants';
 

it('updates a Choice in the list', () => {
  
});

it('updates a Choice in the list', () => {
  
});
